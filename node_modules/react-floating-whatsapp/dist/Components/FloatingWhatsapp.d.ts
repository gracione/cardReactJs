import React from 'react';
interface FloatingWhatsAppProps {
    phoneNumber: string;
    accountName: string;
    height?: number;
    avatar?: string;
    statusMessage?: string;
    chatMessage?: string;
    darkMode?: boolean;
    allowClickAway?: boolean;
    allowEsc?: boolean;
    styles?: React.CSSProperties;
    className?: string;
    placeholder?: string;
    notification?: boolean;
    notificationDelay?: number;
    notificationSound?: boolean;
}
export default function FloatingWhatsApp({ phoneNumber, accountName, height, avatar, statusMessage, chatMessage, darkMode, allowClickAway, allowEsc, styles, className, placeholder, notification, notificationDelay, // 3 minutes
notificationSound }: FloatingWhatsAppProps): JSX.Element;
export {};
